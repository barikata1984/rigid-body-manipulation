# プロジェクト概要

このプロジェクト「Rigid Body Manipulation」は、特にロボットマニピュレーションの文脈における剛体システムのシミュレーションと制御に焦点を当てています。動力学、軌道生成、シミュレーション、可視化のためのモジュールが含まれています。

## 主要技術と使用言語

* **言語**: Python 3.9+
* **主要ライブラリ**: NumPy, SciPy, MuJoCo, dm_control, Tyro, OmegaConf, liegroups, transforms3d
* **バージョン管理**: Git

## コーディング規約とスタイル

* **スタイルガイド**: PEP 8 に準拠します。
* **命名規則**:
  * 変数: `snake_case`
  * 関数: `snake_case`
  * クラス: `CamelCase`
  * 定数: `UPPER_SNAKE_CASE`
* **Docstrings**: 関数とクラスには Google スタイルの Docstring を使用します。
* **型ヒント**: 関数の引数と戻り値には明示的な型ヒントを推奨します。

## ビルドとテストコマンド

* **依存関係管理**: `uv` (例: `uv sync`)
* **テストフレームワーク**: `pytest`
  * **テストの記述**: `unittest.TestCase`を継承したクラスベースのテストを使用します。
  * **テストの実行**: `pytest`コマンドを使用します。
    * 全てのテストを実行: `pytest`
    * 特定のテストを実行: `pytest tests/test_your_module.py`
* **Linting/フォーマット**: `ruff` (例: `ruff check .`, `ruff format .`)

## 重要なディレクトリとファイル

* `main.py`: 実験/シミュレーションの主要なエントリーポイント。
* `configurations/`: 実験用の設定ファイル。
* `trajectories/`: 軌道生成のためのモジュール。
* `simulator/`: シミュレーション環境のセットアップ。
* `tests/`: ユニットテストと結合テスト。
* `xml_models/`: マニピュレーターとオブジェクト用の MuJoCo XML モデル。

## Gemini エージェントへの指示

* **期待する挙動**: あなたに期待する役割は、一般的な会話をするパートナーではなくて、ソフトウェアエンジニアリングやロボット工学を推し進めるための同僚です。ですので、ユーザーの行動を褒める動作をいちいち含める必要はなく、過度に、あるいは適切ではないタイミングでポジティブなことを述べてはいけません。そのような文言はユーザーが取り組んている課題について熟考する際の土台作りには不適切ですし、ノイズにしかなりません。客観的にみて正しいと言えること、また、ユーザーから読み込むように指示のデータに基づいた次議論を行うように徹してください。
* **実装開始タイミング**: 実際にコードを書き始めるタイミングはユーザーが指示します。実装開始をユーザーが明確に指示するまではコードを書き始めることが決して無いように注意してください。実装をリクエストしていないタイミングでコードの新規作成・修正を行われると、実証済みセクションと未実証セクションの区別をつけづらく、デバッグが困難となります。
* **検証の尊守**: コアとなるシミュレーションや制御ロジックを変更する際は、変更を適用する前に必ず関連するテストの実行を提案してください。
* **一貫性**: 既存のコードパターンとアーキテクチャの選択に一貫性を持たせてください。
* **テストコードのスタイル**: 新たなテストコードを追加する際は、既存の`unittest.TestCase`を継承したクラスベースのスタイルに準拠してください。テストの設定には`setUpClass`メソッドを活用し、`tyro`や`omegaconf`などの設定ツールとの連携が必要な場合は、`pytest`の実行フローを考慮した実装を行ってください。浮動小数点数の比較には`numpy.testing.assert_allclose`を使用し、必要に応じて適切な許容誤差（`atol`, `rtol`）を設定してください。
* **外部ライブラリ**: ユーザーの明示的な承認なしに新しい外部ライブラリを導入しないでください。
* **コミットメッセージ**: コミットを提案する際は、Conventional Commits の仕様に従ってください（例: `feat: 新機能の追加`, `fix: バグの修正`）。
